import { createGlobalStyle } from "styled-components"

const GlobalStyles = createGlobalStyle`
    @font-face {
        font-family: 'Roboto', sans-serif; */';
        src: url('../public/path-to-your-font-file.ttf') format('truetype'); /* Font dosyasının yolu */
        font-weight: normal;
        font-style: normal;
    }
`